// the local version of the modle in type script format so that
// the list returned from the API can be displayed properly
export interface Icolour {
  colour: string;
  hexvalue: string;
  value: number;
}
